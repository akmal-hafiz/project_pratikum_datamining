# Commit rules

- Never add a `Co-Authored-By: Claude` (or any AI) trailer to commit messages in this repo.
- Author and committer must always be the user's own git identity — no exceptions.
- Remotes: push to both `origin` (project_pratikum_datamining) and `newrepo` (datamining_pratikum) unless told otherwise.
